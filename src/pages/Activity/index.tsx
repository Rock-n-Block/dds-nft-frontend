import React from 'react';

import { UserActivity } from '../../components/organisms';

const Activity: React.FC = () => {
  return <UserActivity address="" isMultipleFilterValues isAllFilterItem />;
};

export default Activity;
